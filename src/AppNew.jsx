import React, { useEffect, useState } from "react";
import { BrowserRouter, Navigate, Route, Routes, useNavigate } from "react-router-dom";
import { ArrowRight, BarChart3, Check, Mic, ShieldCheck, Sparkles, Target, WandSparkles } from "lucide-react";
import Workspace from "./Workspace";
import { DEMO_USER, isSupabaseConfigured, supabase } from "./lib/supabase";

function Landing() { const navigate = useNavigate(); return <div className="landing-page"><nav className="landing-nav"><div className="brand dark"><div className="brand-mark"><Sparkles size={18} /></div><div><strong>track<span>.</span></strong><small>career workspace</small></div></div><div className="landing-links"><a href="#workflow">Workflow</a><a href="#voice">Voice practice</a><a href="#principles">Principles</a></div><button className="primary-btn" onClick={() => navigate("/login")}>Open workspace <ArrowRight size={15} /></button></nav><section className="landing-hero"><div className="hero-glow" /><div className="hero-copy"><span className="soft-badge"><Sparkles size={13} /> A calmer way to move your career forward</span><h1>Your job search,<br /><em>with a point of view.</em></h1><p>Track every application, prepare for the role in front of you, and practice the conversation out loud—all in one focused workspace.</p><div className="hero-actions"><button className="primary-btn large" onClick={() => navigate("/login")}>Build your workspace <ArrowRight size={16} /></button><a href="#workflow" className="learn-link">See how it works <ArrowRight size={14} /></a></div><div className="trust-line"><ShieldCheck size={15} /> Private by default <span /> No busywork <span /> Built for momentum</div></div><div className="hero-preview"><div className="preview-window"><div className="preview-top"><span /><span /><span /><small>track. / overview</small></div><div className="preview-body"><div className="preview-greeting"><div><small>Good morning, Alex</small><b>A focused view of your search.</b></div><div className="preview-avatar">AM</div></div><div className="preview-stats"><div><small>Active pipeline</small><b>12</b><span>+2 this month</span></div><div><small>Interviews</small><b>04</b><span>+1 this week</span></div><div><small>Readiness</small><b>74%</b><span>Strong foundation</span></div></div><div className="preview-lower"><div className="preview-chart"><small>Pipeline health</small><div className="preview-bars">{[32, 46, 30, 62, 48, 78, 64, 92, 72].map((height, index) => <i key={index} style={{ height: `${height}%` }} />)}</div></div><div className="preview-next"><small>Next up</small><b>Practice your Notion story</b><span>20 min · today</span></div></div></div></div></div></section><section className="logo-row"><span>Designed for thoughtful searches</span><div><b>PRODUCT</b><b>DESIGN</b><b>ENGINEERING</b><b>GROWTH</b><b>LEADERSHIP</b></div></section><section className="landing-section" id="workflow"><div className="section-intro"><span className="eyebrow">One connected workflow</span><h2>From “maybe” to “ready.”</h2><p>Every feature exists to make the next decision clearer, the next conversation stronger, and your search more intentional.</p></div><div className="feature-grid"><Feature icon={<Target />} number="01" title="Track with context" description="Keep statuses, notes, links, compensation, and next steps connected to the opportunity—not scattered across tabs." /><Feature icon={<WandSparkles />} number="02" title="Prepare for the role" description="Turn a job description into a focused brief, likely questions, and a story bank that sounds like you." /><Feature icon={<Mic />} number="03" title="Practice out loud" description="Use a voice-first mock interview, see a useful rubric, and build evidence of your progress through repetitions." /></div></section><section className="landing-section dark-section" id="voice"><div className="voice-section-copy"><span className="eyebrow light">Practice that gives something back</span><h2>Don’t just rehearse.<br /><em>Learn your signal.</em></h2><p>The mock interview coach looks at clarity, relevance, structure, and confidence—then turns one answer into one practical improvement.</p><button className="secondary-btn light-btn" onClick={() => navigate("/login")}>Try the practice room <ArrowRight size={15} /></button></div><div className="voice-card"><div className="voice-card-head"><span className="pulse-dot" />Live practice room <small>private session</small></div><div className="voice-question"><span>QUESTION 2 OF 3</span><b>Walk me through a project that best demonstrates your fit for this role.</b></div><div className="voice-wave">{Array.from({ length: 22 }).map((_, index) => <i key={index} style={{ height: `${18 + ((index * 19) % 44)}px` }} />)}</div><div className="voice-result"><div><small>Latest answer</small><b>78<span>/100</span></b></div><div><small>Coach note</small><p>Make the result more concrete and connect it to the team’s goal.</p></div></div></div></section><section className="principles" id="principles"><div><span className="eyebrow">A better default</span><h2>Less tracking.<br />More traction.</h2></div><div className="principle-list"><div><Check size={16} /><p><b>Make the next step visible.</b><span>Momentum is easier when the smallest action is already written down.</span></p></div><div><Check size={16} /><p><b>Practice with evidence.</b><span>Your strongest answers are stories with decisions, trade-offs, and outcomes.</span></p></div><div><Check size={16} /><p><b>Keep the system quiet.</b><span>A clean workspace should help you think—not add another inbox to manage.</span></p></div></div></section><footer className="landing-footer"><div className="brand dark"><div className="brand-mark"><Sparkles size={16} /></div><strong>track<span>.</span></strong></div><span>Built for the next thoughtful move.</span><button className="text-btn" onClick={() => navigate("/login")}>Open workspace <ArrowRight size={14} /></button></footer></div>; }

function Feature({ icon, number, title, description }) { return <div className="landing-feature"><div className="feature-icon">{icon}</div><span>{number}</span><h3>{title}</h3><p>{description}</p><ArrowRight size={16} /></div>; }

function Login() { const navigate = useNavigate(); const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const signIn = async () => { setLoading(true); if (isSupabaseConfigured) { const { error: authError } = await supabase.auth.signInWithOAuth({ provider: "google", options: { redirectTo: `${window.location.origin}/dashboard` } }); if (authError) setError(authError.message); } else { navigate("/dashboard"); } setLoading(false); }; return <div className="auth-page"><div className="auth-side"><button className="back-link" onClick={() => navigate("/")}><ArrowRight size={14} style={{ transform: "rotate(180deg)" }} />Back to home</button><div className="auth-side-copy"><span className="soft-badge"><Sparkles size={13} /> Your next move starts here</span><h1>Make your search<br /><em>feel lighter.</em></h1><p>A home for the roles you want, the stories you tell, and the confidence you build along the way.</p></div><div className="auth-testimonial"><span>“</span><p>I stopped treating my search like a spreadsheet and started seeing the story it was telling me.</p><small>— Product designer, 2026</small></div></div><div className="auth-main"><div className="auth-card"><div className="brand dark centered"><div className="brand-mark"><Sparkles size={18} /></div><div><strong>track<span>.</span></strong><small>career workspace</small></div></div><h2>Welcome back</h2><p>Sign in to pick up where you left off.</p>{error && <div className="auth-error">{error}</div>}<button className="google-btn" onClick={signIn} disabled={loading}><span className="google-g">G</span>{loading ? "Opening your workspace…" : isSupabaseConfigured ? "Continue with Google" : "Continue to demo workspace"}<ArrowRight size={15} /></button><div className="auth-divider"><span>or</span></div><button className="demo-btn" onClick={() => navigate("/dashboard")}>Explore with sample data <ArrowRight size={15} /></button><p className="auth-note">Your demo data stays in this browser. Connect Supabase for production auth and persistence.</p></div></div></div>; }

function Authenticated() { const [user, setUser] = useState(DEMO_USER); const navigate = useNavigate(); useEffect(() => { if (!supabase) return undefined; supabase.auth.getSession().then(({ data }) => { if (data.session) setUser(data.session.user); }); const { data: listener } = supabase.auth.onAuthStateChange((_, session) => setUser(session?.user || DEMO_USER)); return () => listener.subscription.unsubscribe(); }, []); const signOut = async () => { if (supabase) await supabase.auth.signOut(); setUser(DEMO_USER); navigate("/"); }; return <Workspace user={user} onSignOut={signOut} />; }

export default function AppNew() { return <BrowserRouter><Routes><Route path="/" element={<Landing />} /><Route path="/login" element={<Login />} /><Route path="/dashboard/*" element={<Authenticated />} /><Route path="*" element={<Navigate to="/" replace />} /></Routes></BrowserRouter>; }
