import React from "react";
import { ArrowRight, BarChart3, BookOpen, CalendarDays, ClipboardCheck, LayoutGrid, Sprout } from "lucide-react";
import SectionContainer from "./SectionContainer";
import { SAMPLE_APPLICATIONS, stageFor } from "./landingData";
import StageIcon from "../../StageIcon";

const RECENT = [SAMPLE_APPLICATIONS[2], SAMPLE_APPLICATIONS[1], SAMPLE_APPLICATIONS[0], SAMPLE_APPLICATIONS[3]];

export default function DashboardPreview({ onStart }) {
    return <SectionContainer id="dashboard" variant="sage" className="landing-dashboard-section" eyebrow="04 / THE PRODUCT" title={<>See your entire search<br /><em>growing in one place.</em></>} description="The tracker stays simple. The detail appears when you need it: current stage, preparation progress, and the next useful move."><div className="landing-dashboard-window"><aside className="landing-dashboard-sidebar"><div className="landing-dashboard-brand"><span><Sprout size={14} /></span><b>Career Garden</b></div>{[[LayoutGrid, "Garden"], [ClipboardCheck, "Applications"], [Sprout, "Interview Prep"], [CalendarDays, "Calendar"], [BarChart3, "Analytics"], [BookOpen, "Lessons"]].map(([Icon, label], index) => <button type="button" className={`landing-dashboard-nav ${index === 0 ? "is-active" : ""}`} key={label} onClick={onStart}>{React.createElement(Icon, { size: 15 })}{label}</button>)}<div className="landing-dashboard-side-note"><small>GARDEN HEALTH</small><strong>74%</strong><i><em /></i><span>steady progress</span></div></aside><div className="landing-dashboard-main"><div className="landing-dashboard-toolbar"><div><span>Garden / Overview</span><h3>Good morning, Alex.</h3><p>Your garden is growing. Here is what deserves attention next.</p></div><button type="button" className="landing-small-action" onClick={onStart}>Add role <ArrowRight size={13} /></button></div><div className="landing-dashboard-stats">{[["Applications", "24", "tracked roles"], ["Interviews", "06", "in motion"], ["Offers", "02", "fully bloomed"], ["Garden health", "74%", "real activity"]].map(([label, value, detail]) => <div key={label}><span>{label}</span><strong>{value}</strong><small>{detail}</small></div>)}</div><div className="landing-dashboard-content"><div className="landing-dashboard-map"><div className="landing-dashboard-subhead"><b>Career garden map</b><span>24 plots · 6 stages</span></div><div className="landing-map-surface"><span className="landing-map-path map-path-a" /><span className="landing-map-path map-path-b" />{[["Google", "map-google", stageFor("Interview")], ["Stripe", "map-stripe", stageFor("Screening")], ["Linear", "map-linear", stageFor("Saved")], ["Shopify", "map-shopify", stageFor("Offer")]].map(([company, className, stage]) => <button type="button" className={`landing-map-plot ${className}`} key={company} onClick={onStart}><StageIcon stage={stage} size={22} /><b>{company}</b><small>{stage.gardenName}</small></button>)}</div></div><div className="landing-dashboard-recent"><div className="landing-dashboard-subhead"><b>Recent applications</b><span>View all</span></div>{RECENT.map(application => <button type="button" className="landing-recent-row" key={application.company} onClick={onStart}><span className="landing-recent-icon"><StageIcon stage={application.stage} size={15} /></span><span><b>{application.company}</b><small>{application.role}</small></span><em style={{ color: application.stage.color }}>{application.stage.gardenName}</em><ArrowRight size={13} /></button>)}</div></div></div></div></SectionContainer>;
}
